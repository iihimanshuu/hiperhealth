name: hiperhealth
channels:
  - conda-forge
  - defaults

dependencies:
  - python >=3.10,<=3.13
  - pip
  - poetry
  - nodejs  # used by semantic-release
  - tesseract
  - libmagic
  - pip:
      - paginate==0.5.6  # distlib issue
